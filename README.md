
  # Apply color palette to layout

  This is a code bundle for Apply color palette to layout. The original project is available at https://www.figma.com/design/vMa0Y3vwveBoFRQCtGx2zG/Apply-color-palette-to-layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  